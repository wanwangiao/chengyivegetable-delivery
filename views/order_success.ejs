<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>訂單確認 - 誠憶鮮蔬</title>
  <link crossorigin="" href="https://fonts.gstatic.com/" rel="preconnect"/>
  <link as="style" href="https://fonts.googleapis.com/css2?display=swap&amp;family=Noto+Sans%3Awght%40400%3B500%3B700%3B900&amp;family=Space+Grotesk%3Awght%40400%3B500%3B700" onload="this.rel='stylesheet'" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet"/>
  <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
  <style type="text/tailwindcss">
    :root {
      --primary-50: #f0fdf4;
      --primary-100: #dcfce7;
      --primary-200: #bbf7d0;
      --primary-300: #86efac;
      --primary-400: #4ade80;
      --primary-500: #22c55e;
      --primary-600: #16a34a;
      --primary-700: #15803d;
      --primary-800: #166534;
      --primary-900: #14532d;
      --primary-950: #052e16;
    }
  </style>
  <style>
    body {
      min-height: max(884px, 100dvh);
    }
  </style>
</head>
<body class="bg-gray-50">
<div class="relative flex size-full min-h-screen flex-col justify-between group/design-root overflow-x-hidden" style='font-family: "Space Grotesk", "Noto Sans", sans-serif;'>
<div class="flex-grow">
<header class="sticky top-0 z-10 bg-white shadow-sm">
<div class="flex items-center p-4">
<button onclick="window.history.back()" class="text-gray-700">
<span class="material-symbols-outlined">arrow_back_ios</span>
</button>
<h2 class="text-gray-900 text-lg font-bold flex-1 text-center pr-6">訂單確認</h2>
</div>
</header>
<main class="p-4 sm:p-6">
<div class="text-center mb-6">
<span class="material-symbols-outlined text-7xl text-[var(--primary-500)]">check_circle</span>
<h1 class="text-gray-900 text-2xl font-bold mt-4">恭喜！您的訂單已確認</h1>
<p class="text-gray-600 text-base mt-2">您的訂單編號是 <span class="font-semibold text-gray-800">#<%= order.id %></span></p>
<p class="text-gray-600 text-base">預計將於 <span class="font-semibold text-gray-800">2-3小時內</span> 送達。</p>
</div>

<% if (order.items && order.items.length > 0) { %>
<div class="bg-white rounded-lg shadow-sm p-4 mb-6">
<h3 class="text-gray-900 text-lg font-bold mb-4">訂購商品</h3>
<div class="space-y-4">
<% order.items.forEach(function(item) { %>
<div class="flex items-center gap-4">
<div class="bg-gray-200 bg-center bg-no-repeat aspect-square bg-cover rounded-lg size-16 flex items-center justify-center">
<span class="material-symbols-outlined text-gray-500 text-2xl">local_grocery_store</span>
</div>
<div class="flex-1">
<p class="text-gray-800 text-base font-medium"><%= item.name %></p>
<p class="text-gray-500 text-sm">
  <%= item.quantity %> 
  <% if (item.is_priced_item && item.actual_weight) { %>
    (實重: <%= item.actual_weight %>g)
  <% } else { %>
    個
  <% } %>
</p>
</div>
<p class="text-gray-800 text-base font-medium">$<%= item.line_total %></p>
</div>
<% }); %>
</div>
</div>
<% } %>

<div class="bg-white rounded-lg shadow-sm p-4 mb-6">
<h3 class="text-gray-900 text-lg font-bold mb-4">訂單明細</h3>
<div class="space-y-2">
<div class="flex justify-between">
<p class="text-gray-600">商品總金額</p>
<p class="text-gray-800">$<%= order.subtotal %></p>
</div>
<div class="flex justify-between">
<p class="text-gray-600">運費</p>
<p class="text-gray-800">$<%= order.delivery_fee || 0 %></p>
</div>
<hr class="my-2"/>
<div class="flex justify-between font-bold">
<p class="text-gray-900">總金額</p>
<p class="text-[var(--primary-600)]">$<%= order.total %></p>
</div>
</div>
</div>

<div class="bg-white rounded-lg shadow-sm p-4 mb-6">
<div class="mb-4">
<h3 class="text-gray-900 text-lg font-bold mb-2">送貨地址</h3>
<p class="text-gray-600"><%= order.address %></p>
<% if (order.contact_name) { %>
<p class="text-gray-500 text-sm mt-1">聯絡人：<%= order.contact_name %></p>
<% } %>
<% if (order.contact_phone) { %>
<p class="text-gray-500 text-sm">聯絡電話：<%= order.contact_phone %></p>
<% } %>
</div>
<div>
<h3 class="text-gray-900 text-lg font-bold mb-2">付款方式</h3>
<p class="text-gray-600">
<% if (order.payment_method === 'cash') { %>
現金付款
<% } else if (order.payment_method === 'bank_transfer') { %>
銀行轉帳
<% } else if (order.payment_method === 'line_pay') { %>
LINE Pay
<% } else { %>
現金付款
<% } %>
</p>
</div>
</div>

<% if (order.notes) { %>
<div class="bg-white rounded-lg shadow-sm p-4 mb-6">
<h3 class="text-gray-900 text-lg font-bold mb-2">備註事項</h3>
<p class="text-gray-600"><%= order.notes %></p>
</div>
<% } %>

<!-- LINE 綁定提醒 -->
<% if (typeof sessionLine !== 'undefined') { %>
  <% if (sessionLine) { %>
  <div class="bg-green-50 border border-green-200 rounded-lg p-4 mb-6">
    <div class="flex items-center">
      <span class="material-symbols-outlined text-green-600 mr-2">check_circle</span>
      <p class="text-green-800 text-sm">您的 LINE 已綁定，我們會透過 LINE 通知您訂單狀態。</p>
    </div>
  </div>
  <% } else { %>
  <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-6">
    <div class="flex items-center justify-between">
      <div class="flex items-center">
        <span class="material-symbols-outlined text-yellow-600 mr-2">info</span>
        <p class="text-yellow-800 text-sm">綁定 LINE 接收訂單通知</p>
      </div>
      <a href="/auth/line/login" class="text-[var(--primary-600)] text-sm font-medium">立即綁定</a>
    </div>
  </div>
  <% } %>
<% } %>

</main>
</div>
<footer class="sticky bottom-0 bg-white border-t border-gray-200">
<div class="px-4 py-3 sm:px-6">
<div class="flex flex-col gap-3">
<button onclick="window.location.href='/track-order/<%= order.id %>'" class="flex w-full items-center justify-center rounded-md h-12 px-5 bg-[var(--primary-500)] text-white text-base font-bold tracking-wide hover:bg-[var(--primary-600)] transition-colors">
<span>查看訂單狀態</span>
</button>
<button onclick="window.location.href='/'" class="flex w-full items-center justify-center rounded-md h-12 px-5 bg-gray-100 text-gray-800 text-base font-bold tracking-wide hover:bg-gray-200 transition-colors">
<span>返回首頁</span>
</button>
</div>
</div>
<nav class="flex justify-around border-t border-gray-200 bg-white py-2">
<a class="flex flex-col items-center justify-end gap-1 text-[var(--primary-600)]" href="/">
<span class="material-symbols-outlined">home</span>
<p class="text-xs font-medium">首頁</p>
</a>
<a class="flex flex-col items-center justify-end gap-1 text-gray-500" href="#" onclick="alert('功能開發中')">
<span class="material-symbols-outlined">search</span>
<p class="text-xs font-medium">分類</p>
</a>
<a class="flex flex-col items-center justify-end gap-1 text-gray-500" href="/" onclick="window.scrollTo(0, 0)">
<span class="material-symbols-outlined">shopping_cart</span>
<p class="text-xs font-medium">購物車</p>
</a>
<a class="flex flex-col items-center justify-end gap-1 text-gray-500" href="/track-order/<%= order.id %>">
<span class="material-symbols-outlined">receipt_long</span>
<p class="text-xs font-medium">我的訂單</p>
</a>
<a class="flex flex-col items-center justify-end gap-1 text-gray-500" href="#" onclick="alert('功能開發中')">
<span class="material-symbols-outlined">person</span>
<p class="text-xs font-medium">個人</p>
</a>
</nav>
</footer>
</div>

</body>
</html>