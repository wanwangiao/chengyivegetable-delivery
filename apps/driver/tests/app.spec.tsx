import { describe, it, expect } from 'vitest';

describe.skip('Driver App UI smoke tests', () => {
  it('skipped pending proper React Native test harness', () => {
    expect(true).toBe(true);
  });
});
